<%= form_with(model: review_rating, local: true) do |form| %>
  <% if review_rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review_rating.errors.count, "error") %> prohibited this review_rating from being saved:</h2>

      <ul>
      <% review_rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :rr_type, id: :review_rating_type %>
  </div>

  <div class="field">
    <%= form.label :ratingValue %>
    <%= form.text_field :ratingValue, id: :review_rating_ratingValue %>
  </div>

  <div class="field">
    <%= form.label :bestRating %>
    <%= form.text_field :bestRating, id: :review_rating_bestRating %>
  </div>

  <div class="field">
    <%= form.label :worstRating %>
    <%= form.text_field :worstRating, id: :review_rating_worstRating %>
  </div>

  <div class="field">
    <%= form.label :alternateName %>
    <%= form.text_field :alternateName, id: :review_rating_alternateName %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
